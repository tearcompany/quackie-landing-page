# OpenAI credentials for /api/rewrite
OPENAI_API_KEY=
OPENAI_PROMPT_ID=
OPENAI_PROMPT_VERSION=

# HMAC secret for per-install tokens (never commit a real value)
QUACKIE_SIGNING_SECRET=

# Optional daily rewrite budget per server instance (default: 5000)
# MAX_DAILY_REQUESTS=5000
